# Data Directory

- `raw/`: immutable source datasets.
- `processed/`: transformed outputs generated from raw data.

Keep raw files unchanged and version only lightweight sample data unless large artifacts are managed externally.
